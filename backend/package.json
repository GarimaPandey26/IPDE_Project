{
  "name": "ipde-backend",
  "version": "1.0.0",
  "description": "Backend server for Integrated Platform Data Environment (IPDE) - Simplified Version",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build-production": "npm install && npm install --include=dev --prefix ../frontend && npm run build --prefix ../frontend"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mongoose": "^8.4.1",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.3"
  }
}
